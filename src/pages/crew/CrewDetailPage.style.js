import styled from 'styled-components';

export const CrewDetaiPageBlock = styled.div`
  width: 100%;
  height: calc(100% - 120px);
  overflow: auto;
  margin: 0 auto;

  &::-webkit-scrollbar {
    display:none;
  }
`